# 🧠 Magic Brain — Autonomous Agent System

Полностью автономная система с агентом, инструментами (MCP), векторной памятью (RAG) и веб-интерфейсом.

## 🚀 Быстрый старт

```bash
# 1. Клонировать
git clone <your-repo-url>
cd magic-brain-deploy

# 2. Запустить деплой
./deploy.sh

# 3. Настроить .env (опционально)
cp .env.example .env
# Edit with your keys

# 4. Готово!
# • API: http://localhost:8000
# • WebUI: http://localhost:3000
